package server.repository;

import org.repository.repo.Repository;
import server.model.PrivateChat;
import server.model.PrivateMessage;

public interface PrivateMessageRepository extends Repository<PrivateMessage, PrivateChat> {
}
